# Secrets & credentials
.env
passwords.txt

# Logs
*.log

# Dev/local only (not needed on DreamHost)
node_modules/
scratch/
legacy/
tests/
.agents/
skills/how-todo/
eg

# Firebase backend — deploy via CLI, not git/DreamHost
functions/
firestore.rules
firestore.indexes.json
firebase.json

